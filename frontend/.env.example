# Only needed if the frontend is deployed separately from the backend
# (e.g. Vercel/Netlify frontend + Render/EC2 backend). For local dev
# the vite.config.js proxy handles this automatically.
VITE_API_BASE_URL=http://localhost:5000/api
